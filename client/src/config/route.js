const routes = {
    HOME: '/',
    MENU: 'menu',
    PRODUCT_DETAIL__CATE__SLUG__PID: '/menu/:cate/:slug/:pid',
    LOGIN: 'login',
    RESET_PASSWORD: 'reset-password/:token',
};

export default routes;
